[build]
  command = "npm run build"
  publish = "dist"
  functions = "functions"

# Main API Handler
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200
  force = true

# React Routing (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200